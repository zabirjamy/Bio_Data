# Bio_Data


1. Create Repository
2. create README.md and .gitignore
3. git clone https://github.com/zabirjamy/Bio_Data.git
4. cd folder path
5. setup vs code with github
   - git init
   - git add .
   - git config --global user.name "zabirjamy"
   - git config --global user.email "zabirmahmud1000@gmail.com"
   - git commit -m "Basic Structure of HTML"
   - git push -u origin main